<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="card" style="width: 75%; border:1px solid black; border-radius:15px;">
<div class="card-body">
  <div class="card-title">
    
    <%= form.text_field :title, placeholder:"TITLE" %>
  </div>

  <div class="card-subtitle">
   <br>
    <%= form.rich_text_area :content , placeholder:"CONTENT", style:"text-align:center"%>
  </div>

  <div class="card-subtitle">
    <%= form.submit %>
  </div>
  </div>
<% end %>
